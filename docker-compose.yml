version: "3"
services:

  db:
    image: postgres:11-alpine
    container_name: wikijs_db
    environment:
      POSTGRES_DB: wiki
      POSTGRES_PASSWORD: wikijs21corsair
      POSTGRES_USER: wikijs
    logging:
      driver: "none"
    restart: unless-stopped
    volumes:
      - wikijs-db_vol:/var/lib/postgresql/data

  wiki:
    image: requarks/wiki:2
    container_name: wikijs_app
    depends_on:
      - wikijs_db
    environment:
      DB_TYPE: postgres
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: wikijs
      DB_PASS: wikijs21corsair
      DB_NAME: wiki
    #volumes:
    # - '${LOCAL_PRIVATE_WIKIJS_SSH_KEY_PATH}:${CONTAINER_PRIVATE_WIKIJS_SSH_KEY_PATH}'
    restart: unless-stopped
    ports:
      - "70:3000"

volumes:
  wikijs-db_vol:
    external: true
